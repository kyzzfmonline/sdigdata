# Environment Configuration Template
# Copy this file to create your environment-specific configuration:
#   - Use ./scripts/use-dev.sh for development (Docker)
#   - Use ./scripts/use-prod.sh for production (DigitalOcean)
#   - Use ./scripts/use-local.sh for local development (host machine)
#
# Or manually copy: cp .env.example .env

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL Database Connection URL
# Format: postgresql://username:password@host:port/database
#
# Development (Docker): postgresql://metroform:password@db:5432/sdigdata
# Local (Host):         postgresql://metroform:password@localhost:5432/metroform
# Production:           postgresql://user:password@host:port/sdig_data
DATABASE_URL=postgresql://metroform:password@db:5432/sdigdata

# ============================================================================
# JWT AUTHENTICATION
# ============================================================================
# Secret key for JWT token signing (MUST be changed in production!)
SECRET_KEY=your_secret_key_change_this_in_production

# JWT algorithm (HS256 is standard)
JWT_ALGORITHM=HS256

# Token expiration time in minutes (1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ============================================================================
# FILE STORAGE (MinIO / DigitalOcean Spaces)
# ============================================================================
# S3-compatible storage endpoint
# Development (Docker): http://minio:9000
# Local (Host):         http://localhost:9000
# Production:           https://nyc3.digitaloceanspaces.com
SPACES_ENDPOINT=http://minio:9000

# Storage region
SPACES_REGION=us-east-1

# Bucket name for file storage
SPACES_BUCKET=metroform-dev

# Access key for storage
SPACES_KEY=minio

# Secret key for storage
SPACES_SECRET=miniopass

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
# Allowed origins for Cross-Origin Resource Sharing (comma-separated)
# Include all frontend URLs that will access the API
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001,http://localhost:5173,http://127.0.0.1:5173

# ============================================================================
# ENVIRONMENT
# ============================================================================
# Application environment: development | staging | production
ENVIRONMENT=development

# ============================================================================
# PRODUCTION SETTINGS EXAMPLE
# ============================================================================
# For production, use these settings:
# DATABASE_URL=postgresql://sdig_access:your_password@138.68.143.213:3690/sdig_data
# SECRET_KEY=generate_a_strong_random_secret_key_here
# SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com
# SPACES_REGION=nyc3
# SPACES_BUCKET=your-production-bucket
# SPACES_KEY=your-digitalocean-spaces-key
# SPACES_SECRET=your-digitalocean-spaces-secret
# CORS_ORIGINS=https://your-production-domain.com
# ENVIRONMENT=production
